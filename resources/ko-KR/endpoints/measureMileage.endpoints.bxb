endpoints {
  action-endpoints {
    action-endpoint (measureMileage) {
      accepted-inputs (carName, inputValue, inputUnit, question, startPlace, startPlaceMyPos, endPlace, endPlaceUnit, man)//fuel
      local-endpoint (js/MeasureMileage.js)
    }    
  }
}
