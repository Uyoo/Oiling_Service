endpoints {
  action-endpoints {
    action-endpoint (measureMileage) {
      accepted-inputs (carName, fuel, inputValue, inputUnit, question, startPlace, startPlaceMyPos, endPlace, endPlaceUnit)
      local-endpoint (js/MeasureMileage.js)
    }    
  }
}
